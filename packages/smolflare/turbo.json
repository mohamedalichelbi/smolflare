{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"outputs": ["dist/**"]
		},
		"test": {
			"env": [
				"GOOGLE_APPLICATION_CREDENTIALS",
				"GOOGLE_CLOUD_PROJECT",
				"SMOLFLARE_GCS_TEST_BUCKET"
			]
		}
	}
}
